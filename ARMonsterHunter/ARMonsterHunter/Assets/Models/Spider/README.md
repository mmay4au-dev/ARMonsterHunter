# Spider Model

Place `Spider.usdz` in this folder and add it to the `ARMonsterHunter` target in Xcode.

RealityKit loads `.usdz` directly. If your spider model is `.glb` or `.gltf`, convert it to `.usdz` first, then name the converted file `Spider.usdz`.
