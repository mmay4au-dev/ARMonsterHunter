# Zombie Model

Later, place `Zombie.usdz` in this folder and add it to the `ARMonsterHunter` target in Xcode.

RealityKit loads `.usdz` directly. If your zombie model is `.glb` or `.gltf`, convert it to `.usdz` first, then name the converted file `Zombie.usdz`.
